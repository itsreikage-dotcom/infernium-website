# Infernium Network Website

Static English website built with HTML, CSS and JavaScript for GitHub Pages.

## GitHub Pages
Upload all files and folders from this directory directly to the repository root. `index.html` must remain in the root.

Then open **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/ (root)`.
