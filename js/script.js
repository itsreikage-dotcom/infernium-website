const header=document.querySelector('.site-header');window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',window.scrollY>20));
const menu=document.querySelector('.menu-btn'),nav=document.querySelector('.nav-links');menu?.addEventListener('click',()=>nav.classList.toggle('open'));nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const toast=document.querySelector('.toast');function notify(msg){if(!toast)return;toast.textContent=msg;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}
document.querySelectorAll('[data-copy-ip]').forEach(btn=>btn.addEventListener('click',async()=>{try{await navigator.clipboard.writeText('play.infernium.org');notify('Server IP copied!')}catch{notify('Copy: play.infernium.org')}}));
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));
async function serverStatus(){const el=document.querySelector('[data-server-status]');if(!el)return;try{const r=await fetch('https://api.mcsrvstat.us/3/play.infernium.org');const d=await r.json();if(d.online){el.innerHTML=`<span class="status-dot"></span>${d.players?.online??0} players online · Java 1.21.11`}else{el.innerHTML='<span class="status-dot offline"></span>Server currently offline'}}catch{el.innerHTML='<span class="status-dot"></span>play.infernium.org · Java 1.21.11'}}serverStatus();
document.querySelectorAll('.tab-btn').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tab-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('[data-shop-panel]').forEach(p=>p.hidden=p.dataset.shopPanel!==b.dataset.tab)}));
